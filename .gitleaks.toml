[extend]
useDefault = true

[allowlist]
description = "Narrow false positives supported by the CI action's Gitleaks version"
regexTarget = "line"
regexes = [
    '''excluded\.token,lease_until''',
    '''DISCORD_APPLICATION_ID=replace_with_your_application_id''',
]
